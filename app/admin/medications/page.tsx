import MedicationManagement from '@/components/admin/MedicationManagement';

export const metadata = {
  title: 'Quản lý Thuốc | Admin Healthcare',
};

export default function MedicationsPage() {
  return <MedicationManagement />;
}
