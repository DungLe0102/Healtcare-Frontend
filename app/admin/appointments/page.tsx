import AppointmentManagement from '@/components/admin/AppointmentManagement';

export const metadata = {
  title: 'Quản lý cuộc hẹn - Admin',
};

export default function AdminAppointmentsPage() {
  return <AppointmentManagement />;
}
