import PayoutManagement from '@/components/admin/PayoutManagement';

export const metadata = {
  title: 'Quản lý Lương Bác sĩ | Healthcare Admin',
};

export default function PayoutsPage() {
  return <PayoutManagement />;
}
