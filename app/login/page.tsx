import Login from '@/components/auth/Login';

export const metadata = {
  title: 'Đăng nhập - Healthcare System',
  description: 'Đăng nhập vào hệ thống y tế',
};

export default function LoginPage() {
  return <Login />;
}
